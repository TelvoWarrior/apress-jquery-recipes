$("body").on("keypress",function(event){

    плохо понимаю что за event

if(String.fromCharCode(event.keyCode)=="w"

    понимаю, что вроде бы, идет проверка нажата ли клавиша "w", но
    вот этого String.fromCharCode(event.keyCode) не понимаю

$("#webd").trigger("mouseenter");//тут был метод .hover(), заменил на .trigger("mouseenter")

    по заданию, вроде бы, достаточно что бы при нажатии на access key была имитация наведения мыши на меню
    это происходит, но при нажатии другой access key для другого меню, первое меню остается в состоянии
    как будто с него не убрали указатель
    т.о. если прожать все access keys то меню будет выглядеть так, как будто на каждый элемент меню
    наведен указатель мыши
    мне это не нравится
    хотелось бы сделать, что бы при нажатии другой access key во всех предыдущих эффект hover пропадал
    
    
    эту задачу решил добавив ко всем элементам меню .trigger("mouseleave")
    решение не лаконичное, наверное, не рациональное, но я получаю желаемый результат

    появилась еще одна вещь, которую я хотел бы исправить
    при нажатии одной access key и наведением реальным указателем на другой элемент меню
    у двух элементов меню присутствует эффект hover
    мне нужно сделать так, что бы в таком случае, при наведении реальным указателем на элемент меню,
    все предыдущие эффекты hover пропадали.

    эту задачу я тоже решил, добавив следующую конструкцию
    let webdHover = true;
        if (webdHover==true){
            $("#pgmng").trigger("mouseleave");
            $("#datab").trigger("mouseleave");
        }

    создается переменная и ей присваивается значение true
    далее конструкция if внутри которой, с других элементов меню убирается эффект hover

    Я предполагаю, что мое решение, далеко от идеала.
    Несмотря на это, я рад, что мне удалось решить поставленную задачу.
    На текущий момент я испытываю чувство удовлетворения что не оставляю задачу не решенной 
    или не откалываю на неопределенный срок (навсегда).

    Остается разобраться с методами о которых я писал в самом начале.
